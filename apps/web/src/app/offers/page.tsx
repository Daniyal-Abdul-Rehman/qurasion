import Link from 'next/link';
import { ArrowRight, CircleDollarSign, Plus } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../components/atoms';
import { WorkspaceTable } from '../../components/molecules';
import InvestorPageShell from '../../components/organisms/InvestorPageShell';

const offers = [['#12345', '4812 Elm Street', '$315,000', 'Countered', 'Sep 04'], ['#12341', '901 South Lamar', '$382,000', 'Accepted', 'Sep 01'], ['#12338', '2709 Maple Avenue', '$280,000', 'Submitted', 'Aug 30'], ['#12329', '1178 Briarwood Lane', '$350,000', 'Draft', 'Aug 28']];
export default function OffersPage() { return <InvestorPageShell eyebrow="Transaction center" title="Offers" description="Track every offer from draft through acceptance, with a complete decision timeline." action={<PrimaryButton className="w-fit px-4 py-2.5 text-sm"><Plus size={16} /> Create offer</PrimaryButton>}><div className="grid gap-4 sm:grid-cols-4">{[['Draft', '2'], ['Submitted', '3'], ['Countered', '1'], ['Accepted', '2']].map(([label, value]) => <div key={label} className="rounded-lg border border-[#DDE2DD] bg-white p-5"><p className="text-xs text-[#66706A]">{label}</p><p className="mt-2 font-display text-3xl">{value}</p></div>)}</div><div className="mt-6"><WorkspaceTable headers={['Offer', 'Property', 'Your offer', 'Status', 'Updated']} >{offers.map(([id, property, amount, status, date]) => <tr key={id} className="hover:bg-[#FBFCFA]"><td className="px-5 py-4 font-mono text-xs text-[#66706A]"><Link href={`/offers/${id.slice(1)}`} className="font-semibold text-[#173D2B]">{id}</Link></td><td className="px-5 py-4 font-semibold">{property}</td><td className="px-5 py-4 font-display text-lg">{amount}</td><td className="px-5 py-4"><StatusBadge tone={status === 'Countered' ? 'warning' : status === 'Accepted' ? 'positive' : 'neutral'}>{status}</StatusBadge></td><td className="px-5 py-4 text-xs text-[#66706A]">{date} <ArrowRight className="ml-3 inline" size={14} /></td></tr>)}</WorkspaceTable></div><div className="mt-5 flex items-center gap-3 rounded-lg bg-[#E7F0F4] p-4 text-sm"><CircleDollarSign size={18} className="text-[#315A6B]" /><span>Offers are visible only to authorized participants and are recorded in an auditable timeline.</span></div></InvestorPageShell>; }
