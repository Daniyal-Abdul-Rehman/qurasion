'use client';
import { useState } from 'react';
import SellerPageShell from '../../../../components/organisms/SellerPageShell';
const items = ['New investor interest', 'New offers', 'Offer updates', 'Document requests', 'Deal tasks', 'New messages'];
export default function SellerNotificationSettingsPage() { const [enabled, setEnabled] = useState(items.map(() => true)); return <SellerPageShell eyebrow="Account settings" title="Notification preferences" description="Choose which seller updates reach you and how quickly."><section className="max-w-2xl rounded-lg border border-[#DDE2DD] bg-white p-6"><div className="divide-y divide-[#E8EBE8]">{items.map((item, index) => <div key={item} className="flex items-center gap-4 py-4 first:pt-0"><div className="flex-1"><p className="text-sm font-semibold">{item}</p><p className="mt-1 text-xs text-[#66706A]">Receive important {item.toLowerCase()}.</p></div><button type="button" aria-label={`Toggle ${item}`} onClick={() => setEnabled(enabled.map((value, itemIndex) => itemIndex === index ? !value : value))} className={`flex h-6 w-11 items-center rounded-full p-1 ${enabled[index] ? 'justify-end bg-[#173D2B]' : 'justify-start bg-[#DDE2DD]'}`}><span className="h-4 w-4 rounded-full bg-white" /></button></div>)}</div><button type="button" className="btn-primary mt-5 px-4 py-2.5 text-sm">Save preferences</button></section></SellerPageShell>; }
