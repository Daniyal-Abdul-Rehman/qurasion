import Link from 'next/link';
import { Bell, LockKeyhole, UserRound } from 'lucide-react';
import SellerPageShell from '../../../components/organisms/SellerPageShell';
const items = [['Seller profile', 'Identity, contact, and verification status', '/seller/settings/profile', UserRound], ['Notifications', 'Offers, investor activity, documents, and closing', '/seller/settings/notifications', Bell], ['Security', 'Password, MFA, and active sessions', '/seller/settings/security', LockKeyhole]] as const;
export default function SellerSettingsPage() { return <SellerPageShell eyebrow="Workspace preferences" title="Settings" description="Manage your seller account, verification, and notification preferences."><div className="grid max-w-3xl gap-3">{items.map(([name, detail, href, Icon]) => <Link href={href} key={name} className="flex items-center gap-4 rounded-lg border border-[#DDE2DD] bg-white p-5 hover:border-[#173D2B]"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#E8F5D3] text-[#31551C]"><Icon size={18} /></span><span className="flex-1"><span className="block font-semibold">{name}</span><span className="mt-1 block text-sm text-[#66706A]">{detail}</span></span><span className="text-[#9AA8A0]">→</span></Link>)}</div></SellerPageShell>; }
