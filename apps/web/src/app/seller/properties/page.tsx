import Link from 'next/link';
import { ArrowRight, Building2, Plus } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../../components/atoms';
import SellerPageShell from '../../../components/organisms/SellerPageShell';
import { sellerProperties } from '../../../lib/seller-data';
export default function SellerPropertiesPage() { return <SellerPageShell eyebrow="Property management" title="My properties" description="Manage property information, listings, investor demand, and transaction activity." action={<Link href="/seller/properties/new" className="btn-primary flex w-fit items-center gap-2 px-4 py-2.5 text-sm"><Plus size={16} /> Add property</Link>}><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{sellerProperties.map((property) => <article key={property.id} className="overflow-hidden rounded-lg border border-[#DDE2DD] bg-white"><div className="h-40 bg-cover bg-center" style={{ backgroundImage: `url(${property.image})` }} /><div className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-2xl">{property.address}</h2><p className="mt-1 text-sm text-[#66706A]">{property.location}</p></div><StatusBadge tone={property.status === 'Listed' ? 'positive' : property.status === 'Under offer' ? 'warning' : 'neutral'}>{property.status}</StatusBadge></div><p className="mt-5 font-display text-2xl">{property.price}</p><div className="mt-4 grid grid-cols-3 gap-2 border-y border-[#E8EBE8] py-3 text-xs"><div><p className="text-[#9AA8A0]">Views</p><p className="mt-1 font-semibold">{property.views}</p></div><div><p className="text-[#9AA8A0]">Investors</p><p className="mt-1 font-semibold">{property.investors}</p></div><div><p className="text-[#9AA8A0]">Offers</p><p className="mt-1 font-semibold">{property.offers}</p></div></div><Link href={`/seller/properties/${property.id}`} className="mt-4 flex items-center justify-between text-sm font-semibold text-[#173D2B]">Manage property <ArrowRight size={15} /></Link></div></article>)}</div><div className="mt-6 flex items-center gap-3 rounded-lg bg-[#E7F0F4] p-4 text-sm"><Building2 size={18} className="text-[#315A6B]" /> Property facts from the platform record remain authoritative; seller updates are preserved for review.</div></SellerPageShell>; }
