'use client';

import { useState } from 'react';
import { FileText, Plus, Upload } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../../components/atoms';
import SellerPageShell from '../../../components/organisms/SellerPageShell';
const initialDocs = [['Property tax records', '123 Main Street', 'Uploaded', 'Sep 02'], ['Purchase agreement', 'Cedar Ridge Duplex', 'Uploaded', 'Sep 01'], ['Roof documentation', 'Cedar Ridge Duplex', 'Requested', 'Due Sep 08'], ['Property disclosure', '123 Main Street', 'Required', '']];
export default function SellerDocumentsPage() { const [docs, setDocs] = useState(initialDocs); return <SellerPageShell eyebrow="Controlled documents" title="Documents" description="Upload property and transaction documents with clear status and access context." action={<PrimaryButton className="w-fit px-4 py-2.5 text-sm"><Plus size={16} /> Upload document</PrimaryButton>}><div className="overflow-x-auto rounded-lg border border-[#DDE2DD] bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-[#F7F8F6] text-[10px] uppercase tracking-[0.12em] text-[#9AA8A0]"><tr>{['Document', 'Property', 'Status', 'Updated', ''].map((header) => <th key={header} className="px-5 py-3 font-semibold">{header}</th>)}</tr></thead><tbody className="divide-y divide-[#E8EBE8]">{docs.map(([name, property, status, date], index) => <tr key={name}><td className="px-5 py-4"><p className="flex items-center gap-2 font-semibold"><FileText size={16} className="text-[#173D2B]" />{name}</p></td><td className="px-5 py-4 text-[#66706A]">{property}</td><td className="px-5 py-4"><StatusBadge tone={status === 'Uploaded' ? 'positive' : 'warning'}>{status}</StatusBadge></td><td className="px-5 py-4 text-xs text-[#66706A]">{date || 'Action needed'}</td><td className="px-5 py-4"><button type="button" onClick={() => index > 1 && setDocs(docs.map((doc, docIndex) => docIndex === index ? [doc[0], doc[1], 'Uploaded', 'Now'] : doc))} className="flex items-center gap-1 text-xs font-semibold text-[#173D2B]"><Upload size={14} /> {status === 'Uploaded' ? 'View' : 'Upload'}</button></td></tr>)}</tbody></table></div><p className="mt-4 text-xs text-[#66706A]">Documents are scanned, access-controlled, versioned, and audit logged by the platform.</p></SellerPageShell>; }
