import Link from 'next/link';
import { ArrowRight, HandCoins, Plus } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../../components/atoms';
import SellerPageShell from '../../../components/organisms/SellerPageShell';
const offers = [['#12345', '123 Main Street', 'Investor #A103', '$315,000', 'Countered', 'Sep 04'], ['#12341', 'Cedar Ridge Duplex', 'Investor #B218', '$465,000', 'Accepted', 'Sep 01'], ['#12338', '123 Main Street', 'Investor #C041', '$320,000', 'Under review', 'Sep 03']];
export default function SellerOffersPage() { return <SellerPageShell eyebrow="Transaction center" title="Offers" description="Review, compare, and respond to investor offers across your properties." action={<PrimaryButton className="w-fit px-4 py-2.5 text-sm"><Plus size={16} /> Create counter</PrimaryButton>}><div className="grid gap-4 sm:grid-cols-4">{[['Pending review', '3'], ['Countered', '1'], ['Accepted', '2'], ['Rejected', '4']].map(([label, value]) => <div key={label} className="rounded-lg border border-[#DDE2DD] bg-white p-5"><p className="text-xs text-[#66706A]">{label}</p><p className="mt-2 font-display text-3xl">{value}</p></div>)}</div><div className="mt-6 overflow-x-auto rounded-lg border border-[#DDE2DD] bg-white"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#F7F8F6] text-[10px] uppercase tracking-[0.12em] text-[#9AA8A0]"><tr>{['Offer', 'Property', 'Investor', 'Offer price', 'Status', 'Received'].map((header) => <th key={header} className="px-5 py-3 font-semibold">{header}</th>)}</tr></thead><tbody className="divide-y divide-[#E8EBE8]">{offers.map(([id, property, investor, price, status, date]) => <tr key={id} className="hover:bg-[#FBFCFA]"><td className="px-5 py-4"><Link href={`/seller/offers/${id.slice(1)}`} className="font-mono text-xs font-semibold text-[#173D2B]">{id}</Link></td><td className="px-5 py-4 font-semibold">{property}</td><td className="px-5 py-4 text-[#66706A]">{investor}</td><td className="px-5 py-4 font-display text-xl">{price}</td><td className="px-5 py-4"><StatusBadge tone={status === 'Accepted' ? 'positive' : status === 'Countered' ? 'warning' : 'neutral'}>{status}</StatusBadge></td><td className="px-5 py-4 text-xs text-[#66706A]">{date}<ArrowRight className="ml-3 inline" size={14} /></td></tr>)}</tbody></table></div><div className="mt-5 flex items-center gap-3 rounded-lg bg-[#E7F0F4] p-4 text-sm"><HandCoins size={17} className="text-[#315A6B]" /> Offer timelines are immutable and every response is recorded for authorized participants.</div></SellerPageShell>; }
