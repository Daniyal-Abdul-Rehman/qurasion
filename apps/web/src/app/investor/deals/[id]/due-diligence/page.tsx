'use client';

import { useState } from 'react';
import { ArrowLeft, Check, FileText, Upload } from 'lucide-react';
import { StatusBadge } from '../../../../../components/atoms';
import InvestorPageShell from '../../../../../components/organisms/InvestorPageShell';
const initialItems = [['Property inspection', 'Completed', true], ['Title search', 'Completed', true], ['Insurance binder', 'In progress', false], ['Environmental review', 'Pending', false], ['Financial review', 'Pending', false], ['Legal review', 'Pending', false]];
export default function DueDiligencePage() { const [items, setItems] = useState(initialItems); return <InvestorPageShell eyebrow="Deal workspace" title="Due diligence" description="Cedar Ridge Duplex · Track every review before financing and closing."><ArrowLeft size={15} className="mb-5 inline" /><span className="ml-2 text-sm font-semibold text-[#173D2B]">Back to deal</span><div className="grid gap-5 xl:grid-cols-[1fr_330px]"><section className="rounded-lg border border-[#DDE2DD] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-[#9AA8A0]">Review checklist</p><h2 className="mt-2 font-display text-2xl">6 diligence items</h2></div><StatusBadge tone="warning">2 / 6 complete</StatusBadge></div><div className="mt-5 divide-y divide-[#E8EBE8]">{items.map(([label, status, complete], index) => <div key={label as string} className="flex items-center gap-3 py-4"><button type="button" onClick={() => setItems(items.map((item, itemIndex) => itemIndex === index ? [item[0], item[1], !item[2]] : item))} className={`flex h-7 w-7 items-center justify-center rounded-full ${complete ? 'bg-[#B7D83D] text-[#173D2B]' : 'border border-[#DDE2DD]'}`}>{complete && <Check size={14} />}</button><div className="flex-1"><p className="text-sm font-semibold">{label as string}</p><p className="mt-1 text-xs text-[#66706A]">Assigned to you · Due Sep {String(5 + index).padStart(2, '0')}</p></div><StatusBadge tone={complete ? 'positive' : status === 'In progress' ? 'warning' : 'neutral'}>{complete ? 'Completed' : status as string}</StatusBadge></div>)}</div></section><aside className="rounded-lg border border-[#DDE2DD] bg-white p-5"><h2 className="font-display text-2xl">Diligence documents</h2><p className="mt-2 text-sm text-[#66706A]">Attach evidence to each completed review.</p><button type="button" className="btn-primary mt-5 flex items-center gap-2 px-4 py-2.5 text-sm"><Upload size={15} /> Upload document</button><div className="mt-5 space-y-2">{['Inspection Report', 'Title Commitment'].map((name) => <div key={name} className="flex items-center gap-2 rounded-md bg-[#F7F8F6] p-3 text-xs"><FileText size={15} className="text-[#173D2B]" /><span className="flex-1">{name}</span><Check size={14} className="text-[#31551C]" /></div>)}</div></aside></div></InvestorPageShell>; }
