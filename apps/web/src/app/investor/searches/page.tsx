'use client';

import { useState } from 'react';
import { Bell, Clock3, MapPin, Plus, Search, Trash2 } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../../components/atoms';
import InvestorPageShell from '../../../components/organisms/InvestorPageShell';
const initialSearches = [{ name: 'Dallas Fix & Flip', location: 'Dallas, TX', criteria: '$200K–$400K · ROI 15%+', frequency: 'Daily', count: 12 }, { name: 'Austin Duplexes', location: 'Austin, TX', criteria: '$300K–$600K · Buy & Hold', frequency: 'Weekly', count: 8 }];
export default function SearchesPage() { const [searches, setSearches] = useState(initialSearches); return <InvestorPageShell eyebrow="Automated discovery" title="Saved searches" description="Get notified when new opportunities meet the criteria you care about." action={<PrimaryButton className="w-fit px-4 py-2.5 text-sm"><Plus size={16} /> New search</PrimaryButton>}><div className="grid gap-4 md:grid-cols-2">{searches.map((search) => <div key={search.name} className="rounded-lg border border-[#DDE2DD] bg-white p-5"><div className="flex items-start justify-between"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-[#E8F5D3] text-[#31551C]"><Search size={17} /></span><button type="button" aria-label={`Delete ${search.name}`} onClick={() => setSearches(searches.filter((item) => item.name !== search.name))} className="text-[#9AA8A0] hover:text-[#C56A42]"><Trash2 size={16} /></button></div><h2 className="mt-5 font-display text-2xl">{search.name}</h2><p className="mt-1 flex items-center gap-1 text-sm text-[#66706A]"><MapPin size={13} /> {search.location}</p><p className="mt-4 text-sm text-[#66706A]">{search.criteria}</p><div className="mt-5 flex items-center justify-between border-t border-[#E8EBE8] pt-4"><span className="flex items-center gap-1 text-xs text-[#66706A]"><Clock3 size={14} /> {search.frequency}</span><StatusBadge tone="positive">{search.count} matches</StatusBadge></div></div>)}</div>{searches.length === 0 && <div className="rounded-lg border border-dashed border-[#B9C5BA] bg-white p-10 text-center"><Search className="mx-auto text-[#9AA8A0]" /><p className="mt-3 font-semibold">No saved searches yet</p><p className="mt-1 text-sm text-[#66706A]">Save a search from Discover to get alerts.</p></div>}<div className="mt-6 flex items-center gap-3 rounded-lg bg-[#E7F0F4] p-4 text-sm"><Bell size={17} className="text-[#315A6B]" /> Alerts are deduplicated and suppressed after you save or dismiss a property.</div></InvestorPageShell>; }
