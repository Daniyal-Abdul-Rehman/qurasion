'use client';

import { useState } from 'react';
import { ArrowRight, Check, Plus, Trophy, X } from 'lucide-react';
import { PrimaryButton, StatusBadge } from '../../../components/atoms';
import InvestorPageShell from '../../../components/organisms/InvestorPageShell';
const properties = [{ name: '4812 Elm Street', market: 'Dallas, TX', price: '$328K', value: '$390K', roi: '18.2%', profit: '$56.4K', risk: 'Medium', match: '96%' }, { name: '901 South Lamar', market: 'Austin, TX', price: '$390K', value: '$465K', roi: '16.4%', profit: '$51.2K', risk: 'Low', match: '91%' }, { name: '2709 Maple Avenue', market: 'Houston, TX', price: '$294K', value: '$350K', roi: '15.7%', profit: '$44.8K', risk: 'Medium', match: '89%' }];
export default function ComparePage() { const [selected, setSelected] = useState(properties); return <InvestorPageShell eyebrow="Decision workspace" title="Compare properties" description="Put shortlisted opportunities side by side and choose the strongest fit for your strategy." action={<PrimaryButton className="w-fit px-4 py-2.5 text-sm"><Plus size={16} /> Add property</PrimaryButton>}><div className="overflow-x-auto rounded-lg border border-[#DDE2DD] bg-white"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#F7F8F6]"><tr><th className="w-44 px-5 py-4 font-mono text-[10px] uppercase tracking-[0.12em] text-[#9AA8A0]">Metric</th>{selected.map((property) => <th key={property.name} className="px-5 py-4"><div className="flex items-start justify-between gap-3"><span><span className="block font-semibold">{property.name}</span><span className="mt-1 block text-xs font-normal text-[#66706A]">{property.market}</span></span><button type="button" aria-label={`Remove ${property.name}`} onClick={() => setSelected(selected.filter((item) => item.name !== property.name))}><X size={15} className="text-[#9AA8A0]" /></button></div></th>)}</tr></thead><tbody className="divide-y divide-[#E8EBE8]">{[['Price', 'price'], ['Estimated value', 'value'], ['Projected ROI', 'roi'], ['Potential profit', 'profit'], ['Risk', 'risk'], ['Your match', 'match']].map(([label, key]) => <tr key={label}><td className="px-5 py-4 text-xs text-[#66706A]">{label}</td>{selected.map((property) => <td key={property.name} className={`px-5 py-4 ${key === 'match' ? 'font-semibold text-[#173D2B]' : 'font-display text-xl'}`}>{property[key as keyof typeof property]}{key === 'match' && <Check size={14} className="ml-1 inline" />}</td>)}</tr>)}</tbody></table></div><section className="mt-5 flex items-start gap-4 rounded-lg bg-[#173D2B] p-5 text-white sm:p-6"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-[#B7D83D] text-[#173D2B]"><Trophy size={18} /></span><div><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-[#B7D83D]">Best fit for you</p><h2 className="mt-2 font-display text-2xl">4812 Elm Street</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-white/65">Strongest match with acceptable risk, target-market alignment, and the highest projected return in this set.</p><button type="button" className="mt-4 flex items-center gap-1 text-sm font-semibold text-[#B7D83D]">Open analysis <ArrowRight size={15} /></button></div></section></InvestorPageShell>; }
