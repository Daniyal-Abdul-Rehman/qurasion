'use client';

import { useState } from 'react';
import { Activity, Info } from 'lucide-react';
import { AnalysisNav } from '../../../../../components/molecules';
import InvestorPageShell from '../../../../../components/organisms/InvestorPageShell';
export default function SensitivityPage() { const [purchase, setPurchase] = useState(325); const [rehab, setRehab] = useState(35); const [arv, setArv] = useState(390); const profit = arv - purchase - rehab - 15; return <InvestorPageShell eyebrow="Analysis · Sensitivity" title="Sensitivity analysis" description="Move the assumptions to see which variables have the greatest effect on the outcome."><AnalysisNav active="Sensitivity" /><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_330px]"><section className="rounded-lg border border-[#DDE2DD] bg-white p-5 sm:p-7"><div className="space-y-7">{[['Purchase price', purchase, setPurchase, 250, 400], ['Renovation cost', rehab, setRehab, 10, 80], ['ARV', arv, setArv, 350, 500]].map(([label, value, setter, min, max]) => <label key={label as string} className="block"><div className="flex justify-between text-sm font-semibold"><span>{label as string}</span><span>${value as number}K</span></div><input type="range" min={min as number} max={max as number} value={value as number} onChange={(event) => (setter as (value: number) => void)(Number(event.target.value))} className="mt-4 w-full accent-[#173D2B]" /><div className="mt-1 flex justify-between text-[10px] text-[#9AA8A0]"><span>${min as number}K</span><span>${max as number}K</span></div></label>)}</div><div className="mt-8 flex items-center gap-2 rounded-md bg-[#E7F0F4] p-3 text-xs"><Info size={14} className="text-[#315A6B]" /> Values are shown in thousands and update the base case model.</div></section><aside className="rounded-lg bg-[#173D2B] p-5 text-white"><Activity className="text-[#B7D83D]" size={19} /><p className="mt-5 font-mono text-[10px] uppercase tracking-[0.14em] text-[#B7D83D]">Live base case</p><p className="mt-2 font-display text-4xl">${profit}K</p><p className="mt-1 text-sm text-white/65">Projected profit</p><div className="mt-6 border-t border-white/15 pt-5"><p className="text-xs text-white/60">Projected ROI</p><p className="mt-1 font-display text-2xl">{Math.round((profit / (purchase + rehab + 15)) * 100)}%</p></div></aside></div></InvestorPageShell>; }
