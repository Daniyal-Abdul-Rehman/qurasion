import Link from 'next/link';
import { ArrowRight, CheckCircle2, FileText } from 'lucide-react';
import { StatusBadge } from '../../components/atoms';
import InvestorPageShell from '../../components/organisms/InvestorPageShell';

const deals = [{ name: 'Cedar Ridge Duplex', location: 'Austin, TX', price: '$485,000', status: 'Due diligence', progress: 65, tasks: 4 }, { name: '901 South Lamar', location: 'Austin, TX', price: '$390,000', status: 'Contracted', progress: 42, tasks: 7 }, { name: '1178 Briarwood Lane', location: 'Dallas, TX', price: '$362,500', status: 'Closing', progress: 86, tasks: 1 }];
export default function DealsPage() { return <InvestorPageShell eyebrow="Transaction workspace" title="My deals" description="Move accepted offers through due diligence, financing, and closing." ><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{deals.map((deal) => <Link href={`/deals/${deal.name.toLowerCase().replaceAll(' ', '-').replaceAll(',', '')}`} key={deal.name} className="group rounded-lg border border-[#DDE2DD] bg-white p-5 transition hover:-translate-y-1 hover:border-[#173D2B]"><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-[#E8F5D3] text-[#31551C]"><CheckCircle2 size={18} /></div><StatusBadge tone={deal.progress > 80 ? 'positive' : 'warning'}>{deal.status}</StatusBadge></div><h2 className="mt-6 font-display text-2xl">{deal.name}</h2><p className="mt-1 text-sm text-[#66706A]">{deal.location}</p><p className="mt-5 font-display text-2xl">{deal.price}</p><div className="mt-5"><div className="flex justify-between text-xs"><span className="text-[#66706A]">Deal progress</span><span className="font-semibold">{deal.progress}%</span></div><div className="mt-2 h-2 rounded-full bg-[#E8EBE8]"><div className="h-2 rounded-full bg-[#B7D83D]" style={{ width: `${deal.progress}%` }} /></div></div><div className="mt-5 flex items-center justify-between border-t border-[#E8EBE8] pt-4 text-xs text-[#66706A]"><span>{deal.tasks} tasks remaining</span><ArrowRight size={15} className="text-[#173D2B]" /></div></Link>)}</div><div className="mt-8 rounded-lg border border-dashed border-[#B9C5BA] bg-[#FBFCFA] p-8 text-center"><FileText className="mx-auto text-[#9AA8A0]" size={22} /><p className="mt-3 font-semibold">Accepted offers become deals here</p><p className="mt-1 text-sm text-[#66706A]">Keep your transaction team, tasks, and documents in one place.</p></div></InvestorPageShell>; }
