import Link from 'next/link';
import { Bell, CreditCard, LockKeyhole, SlidersHorizontal, UserRound } from 'lucide-react';
import InvestorPageShell from '../../components/organisms/InvestorPageShell';
const settings = [['Profile', 'Personal and account information', '/settings/profile', UserRound], ['Investment criteria', 'Markets, returns, and strategy preferences', '/settings/investment-criteria', SlidersHorizontal], ['Notifications', 'Choose what reaches you and where', '/settings/notifications', Bell], ['Security', 'Password, MFA, and active sessions', '/settings/security', LockKeyhole], ['Billing', 'Plan, invoices, and payment methods', '/settings/billing', CreditCard]] as const;
export default function SettingsPage() { return <InvestorPageShell eyebrow="Workspace preferences" title="Settings" description="Manage your account, investment preferences, and notification controls."><div className="grid max-w-3xl gap-3">{settings.map(([label, detail, href, Icon]) => <Link href={href} key={label} className="flex items-center gap-4 rounded-lg border border-[#DDE2DD] bg-white p-5 transition hover:border-[#173D2B]"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#E8F5D3] text-[#31551C]"><Icon size={18} /></span><span className="flex-1"><span className="block font-semibold">{label}</span><span className="mt-1 block text-sm text-[#66706A]">{detail}</span></span><span className="text-[#9AA8A0]">→</span></Link>)}</div></InvestorPageShell>; }
