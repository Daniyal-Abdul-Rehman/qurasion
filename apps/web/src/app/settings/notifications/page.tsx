'use client';

import { useState } from 'react';
import { Bell, Check } from 'lucide-react';
import { PrimaryButton } from '../../../components/atoms';
import InvestorPageShell from '../../../components/organisms/InvestorPageShell';
const preferences = ['New matches', 'Property updates', 'Analysis completion', 'Offer updates', 'Deal updates', 'Document updates', 'Messages', 'Market alerts'];
export default function NotificationSettingsPage() { const [enabled, setEnabled] = useState(preferences.map(() => true)); return <InvestorPageShell eyebrow="Account settings" title="Notification preferences" description="Choose which updates reach you and how quickly you receive them."><section className="max-w-3xl rounded-lg border border-[#DDE2DD] bg-white p-5 sm:p-6"><div className="flex items-center gap-3 border-b border-[#E8EBE8] pb-5"><Bell className="text-[#173D2B]" /><div><h2 className="font-display text-2xl">Updates and alerts</h2><p className="text-sm text-[#66706A]">In-app notifications are always available.</p></div></div><div className="mt-2 divide-y divide-[#E8EBE8]">{preferences.map((preference, index) => <div key={preference} className="flex items-center gap-4 py-4"><div className="flex-1"><p className="text-sm font-semibold">{preference}</p><p className="mt-1 text-xs text-[#66706A]">Receive important changes about {preference.toLowerCase()}.</p></div><button type="button" aria-pressed={enabled[index]} onClick={() => setEnabled(enabled.map((value, itemIndex) => itemIndex === index ? !value : value))} className={`flex h-6 w-11 items-center rounded-full p-1 transition ${enabled[index] ? 'justify-end bg-[#173D2B]' : 'justify-start bg-[#DDE2DD]'}`}><span className="h-4 w-4 rounded-full bg-white" /></button></div>)}</div><PrimaryButton className="mt-5 px-4 py-2.5 text-sm"><Check size={16} /> Save preferences</PrimaryButton></section></InvestorPageShell>; }
